# Render Mode (static | ssr | isr)
# static: Static Export - 完全静的HTMLを out/ に出力（S3, GitHub Pages等にデプロイ可能）
# ssr: Server-Side Rendering - Node.jsサーバーでリクエスト毎にレンダリング
# isr: Incremental Static Regeneration - 静的生成 + 一定間隔で再生成
NEXT_PUBLIC_RENDER_MODE=static

# ISR Revalidate Interval (seconds) - ISRモード時のみ有効
NEXT_PUBLIC_REVALIDATE_INTERVAL=3600

# API Base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api

# Enable Development Tools
NEXT_PUBLIC_ENABLE_DEVTOOLS=true

# Enable MSW (Mock Service Worker) for Development
# true: Development環境でモックサーバーを使用
# false: 実際のAPIサーバーに接続
NEXT_PUBLIC_ENABLE_MSW=true
